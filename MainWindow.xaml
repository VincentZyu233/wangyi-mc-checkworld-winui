<Window
    x:Class="WangyiMCCheckworld.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WangyiMCCheckworld"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Title="网易MC检查世界"
    Width="1100"
    Height="700"
    MinWidth="800"
    MinHeight="500"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title Bar -->
        <Grid Grid.Row="0" Height="48" ColumnDefinitions="Auto,*,Auto">
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                <TextBlock Text="🎮" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="网易MC检查世界" 
                           FontSize="14" 
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextFillColorPrimaryBrush}"/>
            </StackPanel>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="MinimizeButton"
                        Style="{StaticResource WindowButtonStyle}"
                        Content="&#xE921;"
                        Click="MinimizeButton_Click"/>
                <Button x:Name="MaximizeButton"
                        Style="{StaticResource WindowButtonStyle}"
                        Content="&#xE922;"
                        Click="MaximizeButton_Click"/>
                <Button x:Name="CloseButton"
                        Style="{StaticResource WindowButtonStyle}"
                        Content="&#xE8BB;"
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
        
        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="16,8,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Search Box -->
            <muxc:TextBox x:Name="SearchBox"
                          Grid.Column="0"
                          PlaceholderText="搜索存档..."
                          MaxWidth="300"
                          HorizontalAlignment="Left"
                          TextChanged="SearchBox_TextChanged">
                <muxc:TextBox.Icon>
                    <FontIcon Glyph="&#xE721;" FontSize="16"/>
                </muxc:TextBox.Icon>
            </muxc:TextBox>
            
            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <muxc:Button x:Name="RefreshButton"
                             Content="刷新"
                             Click="RefreshButton_Click">
                    <muxc:Button.Icon>
                        <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                    </muxc:Button.Icon>
                </muxc:Button>
                <muxc:Button x:Name="BackupButton"
                             Content="备份"
                             Click="BackupButton_Click">
                    <muxc:Button.Icon>
                        <FontIcon Glyph="&#xE8B7;" FontSize="16"/>
                    </muxc:Button.Icon>
                </muxc:Button>
            </StackPanel>
        </Grid>
        
        <!-- Main Content - DataGrid -->
        <Grid Grid.Row="2" Margin="16,0,16,0">
            <muxc:DataGrid x:Name="WorldDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserReorderColumns="True"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Background="Transparent"
                          BorderThickness="0">
                <muxc:DataGrid.Columns>
                    <muxc:DataGridTextColumn Header="文件夹名" Binding="{Binding FolderName}" Width="200"/>
                    <muxc:DataGridTextColumn Header="世界名称" Binding="{Binding WorldName}" Width="200"/>
                    <muxc:DataGridTextColumn Header="最后保存" Binding="{Binding LastModified}" Width="150"/>
                    <muxc:DataGridTextColumn Header="大小" Binding="{Binding Size}" Width="100"/>
                    <muxc:DataGridTemplateColumn Header="操作" Width="200">
                        <muxc:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <muxc:Button Content="重命名"
                                                 Padding="8,4"
                                                 Click="RenameButton_Click"/>
                                    <muxc:Button Content="删除"
                                                 Padding="8,4"
                                                 Click="DeleteButton_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </muxc:DataGridTemplateColumn.CellTemplate>
                    </muxc:DataGridTemplateColumn>
                </muxc:DataGrid.Columns>
            </muxc:DataGrid>
            
            <!-- Loading Indicator -->
            <muxc:ProgressRing x:Name="LoadingRing"
                              IsActive="False"
                              Visibility="Collapsed"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
        </Grid>
        
        <!-- Status Bar -->
        <Grid Grid.Row="3" Margin="16,8,16,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="StatusText"
                       Grid.Column="0"
                       Text="就绪"
                       Foreground="{StaticResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"/>
            
            <TextBlock x:Name="VersionText"
                       Grid.Column="1"
                       Text="v1.0.0"
                       Foreground="{StaticResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"/>
        </Grid>
    </Grid>
    
    <Window.Resources>
        <!-- Window Control Button Style -->
        <Style x:Key="WindowButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="{StaticResource TextFillColorPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ControlFillColorDefaultBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ControlStrongFillColorDefaultBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>